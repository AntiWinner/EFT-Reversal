using UnityEngine;

public class PostprocessGrayscale : Posteffect
{
	protected override void OnRenderImage(RenderTexture source, RenderTexture destanation)
	{
		base.OnRenderImage(source, destanation);
	}
}
